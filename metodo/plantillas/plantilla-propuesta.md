# Plantilla de propuesta comercial

Documento que se le envía a la empresa después del **contacto inicial**, para
plantear el programa y cerrar el arranque. Deriva de
[../../publico/el-servicio.md](../../publico/el-servicio.md).

> Se vende la relación (un sistema vivo que se construye y opera), no un software
> suelto. SOE **no tiene precio de catálogo**: este plan y su presupuesto salen del
> análisis hecho en el contacto inicial, no de una lista de precios.

---

# Propuesta — `<empresa>`

**Fecha:** `<fecha>` · **Para:** `<contacto/rol>`

## Situación actual

Breve descripción del dolor que motiva la asesoría (qué usan hoy, qué les cuesta,
qué no les resuelve). Anclar contra un costo actual si existe.

`<...>`

## Cómo trabajamos

Un acompañamiento como **área de sistemas y digitalización tercerizada**, que
siempre empieza entendiendo la empresa. SOE es una **metodología**, no un producto
enlatado: este plan y su presupuesto salen del análisis del contacto inicial.

## Plan para `<empresa>`

Elegí **un** modelo de Fase 1 según el cliente (ver `el-servicio.md`). Borrar el
que no aplique.

### Modelo A — Bolsa de horas mensual

- **Programa:** bolsa de `<n>` hs/mes × `<rate>` = **`<monto>`/mes**, sin contrato
  ni permanencia (mes a mes).
- **Foco inicial:** `<el frente prioritario acordado>`.
- **Horas flexibles:** además del desarrollo, cubren consultas/asesorías,
  capacitación (ej. herramientas de IA) y documentación. Se dedican a lo que más
  valor genere cada mes.
- **Diagnóstico:** no es una fase pagada aparte; el relevamiento inicial se absorbe
  en las primeras horas del programa.

### Modelo B — Diagnóstico + retainer con builds por valor

- **Fase 0 — Diagnóstico:** `<n>` semanas, **`<monto>` (único, precio fijo)**.
  Entregable: arranque del SOE (`<empresa>-soe`) + **roadmap** priorizado.
- **Fase 1 — Programa:**
  - **Retainer base:** `<monto>/mes` — modelado, asesoría, mantenimiento y
    evolución del SOE.
  - **Builds por valor:** los desarrollos grandes se cotizan por su valor cuando el
    roadmap los activa; el mes sube durante el build y vuelve al base.

## Qué se lleva `<empresa>`

Un activo vivo, versionado y portable (su SOE), que le pertenece: conocimiento,
procesos, herramientas y automatizaciones, operados y en evolución.

## Próximos pasos

1. Acordar el plan.
2. Agendar el arranque (`<foco inicial>`) con `<stakeholders>`.

---

_Opcional (Modelo B): el diagnóstico se acredita al primer mes del programa si se
continúa dentro de `<n>` días._
